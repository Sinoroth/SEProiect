@model IEnumerable<MVC.Models.RemainingDebt>

@{
    ViewBag.Title = "List";
}

<h2>RemainingDebts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Apartment.ApartmentNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DebtTo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Month)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AmountOfMoneyOwed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Apartment.ApartmentNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DebtTo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Month)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AmountOfMoneyOwed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RemainingDebtId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RemainingDebtId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RemainingDebtId })
        </td>
    </tr>
}

</table>
